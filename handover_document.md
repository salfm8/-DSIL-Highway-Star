# 🚀 Project Handover Document: Smart Cinematic DoF

이 문서는 주행 영상에 객체 추적 기반 시네마틱 DoF(피사계 심도) 효과를 적용하는 "Smart DOF" 프로젝트의 인수인계 요약서입니다. 다음 AI 어시스턴트가 프로젝트의 맥락을 완벽히 이해하고 즉시 작업을 이어나갈 수 있도록 작성되었습니다.

---

## 1. 프로젝트 개요 및 목표

### 🎯 목적과 핵심 기능
- **목적**: 차량 주행 영상(대시캠 등)에서 특정 피사체(예: 롯데마트 간판, 특정 건물)에 시네마틱한 포커스를 맞추고, 피사체가 화면에서 사라지면 자연스럽게 배경 전체가 선명해지는(Deep Focus) **스마트 포커스 풀링(Focus Pulling)** 효과를 자동으로 구현.
- **핵심 기능**:
  - **객체 추적**: Optical Flow를 이용해 초기 타겟 픽셀 위치를 연속 추적.
  - **객체 분리(Segmentation)**: AI 모델을 활용해 타겟 픽셀이 속한 피사체의 정확한 외곽선 추출.
  - **Depth-aware Bokeh**: 뎁스맵을 활용해 거리에 비례하는 자연스러운 아웃포커싱 효과 적용.
  - **자연스러운 트랜지션**: 피사체가 시야를 벗어날 때 부자연스러운 끊김 없이 스무스하게 심도가 깊어지는 연출.

### 🛠 기술 스택 및 환경
- **언어**: Python 3.11 (Miniconda 환경)
- **핵심 라이브러리**:
  - `OpenCV (cv2)`: 영상 I/O, Optical Flow (Lucas-Kanade) 추적, 가우시안 블러 및 이미지 합성.
  - `transformers` (`pipeline`): 단일 이미지 뎁스 추정 (`LiheYoung/depth-anything-base-hf` 모델 활용).
  - `ultralytics (SAM)`: 객체의 의미론적 마스크 추출을 위한 MobileSAM (`mobile_sam.pt`).
  - `numpy`, `torch`

---

## 2. 주요 개발 흐름 및 구현 내용

### 🌊 파이프라인 변천사
- **V1 ~ V4 (물리 기반 CoC 렌더링)**
  - 카메라 렌즈 공식(Circle of Confusion)과 모노큘러 뎁스맵을 기반으로 피사계 심도를 시뮬레이션함.
  - **한계**: 같은 깊이(Depth)에 있는 다른 피사체들까지 모두 선명해져 버려, 사용자가 원하는 "특정 건물 객체만 포커스" 요구사항을 충족시키지 못함.
- **V5 (Flood Fill 기반 마스크)**
  - 추적된 좌표 주변으로 뎁스 차이가 적은 영역만 `cv2.floodFill`로 묶어 마스크를 씌우려 시도.
  - **한계**: 뎁스맵 특성상 거리가 비슷한 주변 육교나 나무가 한 덩어리로 인식되어 여전히 배경 분리에 실패함.
- **🌟 V6 (현재 최종 모델 - `cinematic_v6_sam.py`)**
  - **설계 의도**: 완벽한 피사체 분리를 위해 SAM(Segment Anything Model) 도입.
  - **구조**:
    1. `Optical Flow`로 간판 좌표 `(x, y)` 추적.
    2. 해당 좌표를 `MobileSAM` 프롬프트로 전달해 "건물 외곽선 마스크" 획득.
    3. 마스크 내부는 선명도(Blur 0) 강제 유지, 외부는 뎁스 맵 차이에 비례한 블러 처리.
    4. 추적 실패(화면 이탈) 시, **마지막으로 렌더링된 배경 블러 상태**를 메모리에 저장하고, 그 블러 강도를 서서히 0(선명)으로 낮춰가는 점진적 포커스 풀링 구현.

---

## 3. 트러블슈팅 및 결정 사항 (중요 🚨)

### 🐛 주요 문제 해결 내역
1. **객체 분리 실패 (Depth Map의 한계)**
   - **증상**: 롯데마트 건물에 포커스를 맞추면 옆에 있는 육교도 같이 선명해짐.
   - **시도 및 실패**: CoC 물리 공식 조절, 뎁스 허용 오차 감소, Flood Fill 시도 모두 실패.
   - **최종 결정**: 뎁스만으로는 의미론적(Semantic) 분리가 불가능하므로, 픽셀 좌표를 기반으로 **SAM 모델을 호출하여 마스크를 결합하는 하이브리드 방식** 채택.
2. **트랜지션 "튀는" 현상 (부자연스러운 포커스 풀링)**
   - **증상**: 타겟이 시야에서 사라질 때, 즉시 전체가 선명해지거나(띡 하고 바뀜), 화면 전체를 갑자기 심하게 블러 처리한 뒤 서서히 걷히게 짜여져 있어 시각적 불쾌감 유발.
   - **최종 결정**: 객체 이탈 직전 프레임의 **'배경 블러 맵(마스크 바깥쪽)'의 강도를 계수로 관리(`current_blur_strength = 1.0 -> 0.0`)하여 선형적으로 줄여가는 방식** 수용. 
3. **Numpy & Matplotlib 버전 충돌 에러**
   - **증상**: `ultralytics` 로딩 시 `ImportError: numpy.core.multiarray failed to import` 발생. (Numpy 2.x와 오래된 Matplotlib 간 충돌)
   - **해결**: `pip install "numpy>=2" --upgrade matplotlib`로 Matplotlib를 최신화하여 해결함. 임의로 Numpy를 1.x로 내리면 OpenCV 5.x와 충돌하므로 **Numpy 2.x 유지 필수**.

---

## 4. 현재 진행 상태 및 남은 과제 (To-Do)

### 📌 현재 소스 코드 상태
- `cinematic_v6_sam.py`가 성공적으로 작성되어 영상 렌더링(`step10_v6_sam.mp4`) 및 V5와의 비교 영상(`phase10_video_comparison.mp4`) 생성을 완료했습니다.
- 사용자가 마지막 결과물(V6)을 확인하고 피드백을 주기 직전 단계입니다.

### 📝 진행 대기 중인 우선순위 작업
1. **사용자 피드백 반영**: V6 영상 시청 후 마스크 경계의 부드러움(Edge Smoothing)이나 트랜지션 속도(`transition_speed = 0.05`) 미세 조정을 요청받을 가능성이 높습니다.
2. **최적화 고려**: 현재 매 프레임마다 MobileSAM 추론이 들어가므로(약 145초 소요/268프레임), 실시간성이나 속도 향상이 필요하다면 프레임 간 마스크 보간법이나 SAM 추론 주기 조절이 필요할 수 있습니다.
3. **오디오 맵핑 (선택)**: 초기 기획 파일(`cinematic_audio_mapping.py`)에 기반해 영상의 오디오 트랙을 살리거나 합치는 후반 작업.

---

## 5. 다음 AI를 위한 가이드

- **시각적 완성도 우선**: 사용자는 시네마틱한 연출과 "자연스러움"에 기준이 매우 높습니다. 값이 변할 때 갑자기 튀는 컷 편집 방식보다는 항상 **부드러운 상태 보간(Interpolation)**을 신경 써야 합니다.
- **Depth 맹신 금지**: "특정 객체"를 지정받았다면 단순 Depth 차이 연산으로는 절대 객체만 깔끔하게 분리되지 않습니다. 반드시 현재 설정된 SAM 추론 파이프라인을 유지하거나 더 가벼운 Segmentation 모델을 함께 활용하세요.
- **코드 검증 습관**: 시각적인 마스크 처리 코드를 수정할 경우, 전체 영상을 렌더링하기 전에 `cv2.imwrite` 등을 통해 첫 프레임의 마스크와 블러 맵을 파일로 출력(Debug Image)하여 의도한 영역이 정확히 잡히는지 확인하는 습관이 시행착오를 크게 줄여줍니다. (이전 디버그용 스크립트 `debug_mask_test.py` 참고)
